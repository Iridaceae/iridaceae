bot contains Iris, which is interaction between discord and the bot
db contains all database schenanigans (DAO, Mongo, and maybe SQL in the future)
log contains IrisLogger that wraps sirupen/logrus

features:
!ir 
!ir simp @givenuser
--> discordID 
--> assign people to people

database:
if we want to save statistics --> CRUD with `mgo` --> lives inside db/mongo.go and db/db.go

    model:
    - Users -> save user model and just update pomodoroTime saved under minutesStudied
    - Pomodoro -> maybe just have to save it in cache

